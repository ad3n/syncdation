server:
  cert:
    ca_cert: certs/ca-cert.pem
    cert_file: certs/server-cert.pem
    cert_key: certs/server-key.pem
  plugin_dir: plugins # Plugins directory
  files:
    log_atm:
      port: 8181 # Port to open
      directory: samples/destinations # File store directory
      exist: rename
      timestamp: false
  dbs:
    main:
      port: 8183 # Port to open
      driver: mysql # Database driver
      db:
        host: localhost
        port: 3306
        user: root
        password: aden
        dbname: todo
      table: todo2 # Table name
      columns: # Columns list
        - id
        - title
        - done 
