rest:
  db:
    driver: mysql
    host: localhost
    port: 3306
    user: root
    password: aden
    dbname: skeleton
  paths:
    get_all:
      path: /todos
      type: sql
      sql: samples/sqls/todo_rest.sql
      defaults:
        done: 0
    files:
      path: /files
      type: file
      directory: samples/files
